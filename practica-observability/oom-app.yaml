apiVersion: v1
kind: Pod
metadata:
  name: oom-test
spec:
  containers:
    - name: stress
      image: polinux/stress
      resources:
        limits:
          memory: "300Mi"
      command:
        - stress
        - --vm
        - "1"
        - --vm-bytes
        - "200M"
