apiVersion: v1
kind: Pod
metadata:
  name: crash-app
spec:
  containers:
    - name: app
      image: busybox
      # termina inmediatamente con error
      # command: ["sh", "-c", "exit 1"]
      command: ["sh", "-c", "sleep 3600"]
