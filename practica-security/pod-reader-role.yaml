apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: rbac-lab
  name: pod-reader
rules:
  # api version group
  - apiGroups: [""]
    # nombre del o los recursos
    resources: ["pods"]
    # acciones permitidas sobre dichos recursos
    verbs: ["get", "watch", "list"]
